#pragma once

// The sound object. Properties like looping and volume, and functions like stop

/*
namespace Egl {
	class AudioSample {
	public:
		AudioSample(const std::string& path) : path(path) {}
		~AudioSample();
	private:
		std::string path;
	};
}
*/